{
  "path": "/Users/tranquangdang21/Projects/fast_file_search/crates/ffs-core/src/types.rs",
  "mode": "outline",
  "body": "<anonymous> use std::io::Read;\n<anonymous> use std::path::{Path, PathBuf};\n<anonymous> use std::sync::OnceLock;\n<anonymous> use std::sync::atomic::{AtomicI32, AtomicU64, AtomicUsize, Ordering};\n<anonymous> use crate::constraints::Constrainable;\n<anonymous> use crate::query_tracker::QueryMatchEntry;\n<anonymous> use crate::simd_path::{ArenaPtr, PATH_BUF_SIZE};\n<anonymous> use ffs_query_parser::{FfsQuery, FuzzyQuery, Location};\nFfsStringStorage pub trait FfsStringStorage {\nFileSliceExt pub trait FileSliceExt {\nFileContent pub enum FileContent {\nFileItemFlags pub struct FileItemFlags;\nDirFlags pub struct DirFlags;\nDirItem pub struct DirItem {\nFileItem pub struct FileItem {\nMMAP_THRESHOLD const MMAP_THRESHOLD: u64 = 16 * 1024;\nMMAP_THRESHOLD const MMAP_THRESHOLD: u64 = 4 * 1024;\nFRESH_MMAP_THRESHOLD pub(crate) const FRESH_MMAP_THRESHOLD: u64 = 1024 * 1024;\nFRESH_MMAP_THRESHOLD pub(crate) const FRESH_MMAP_THRESHOLD: u64 = 0;\nFRESH_MMAP_THRESHOLD pub(crate) const FRESH_MMAP_THRESHOLD: u64 = 256 * 1024;\nMmapSlot pub type MmapSlot = Option<memmap2::Mmap>;\nMmapSlot pub type MmapSlot = ();\nload_file_content fn load_file_content(path: &Path, size: u64) -> Option<FileContent> {\nScore pub struct Score {\nPaginationArgs pub struct PaginationArgs {\nScoringContext pub struct ScoringContext<'a> {\nSearchResult pub struct SearchResult<'a> {\nDirSearchResult pub struct DirSearchResult<'a> {\nMixedItemRef pub enum MixedItemRef<'a> {\nMixedSearchResult pub struct MixedSearchResult<'a> {\nMAX_MMAP_FILE_SIZE const MAX_MMAP_FILE_SIZE: u64 = 10 * 1024 * 1024;\nMAX_CACHED_CONTENT_BYTES const MAX_CACHED_CONTENT_BYTES: u64 = 512 * 1024 * 1024;\nContentCacheBudget pub struct ContentCacheBudget {\n",
  "kept_bytes": 1687,
  "footer_bytes": 0,
  "view": "signatures"
}
